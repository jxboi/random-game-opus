# CLAUDE.md

Project guidance for Claude Code lives in AGENTS.md (shared with other agents):

@AGENTS.md

Quick start: `npm test` (headless), `npm start` (serve on :8000), `npm run smoke` (browser checks → `shots/`).
